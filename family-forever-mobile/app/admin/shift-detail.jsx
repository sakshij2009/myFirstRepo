// Admin/owner route for the Shift Detail + report screen.
// Renders the same screen component as the staff app, but registered under the
// /admin stack so the back button returns to the owner pages (not the staff app).
export { default } from "../shift-detail";
